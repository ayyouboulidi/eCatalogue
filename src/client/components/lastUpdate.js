import React, { Component } from 'react';

export default class lastUpdate extends Component {
    render(){
        return(
            <div>
              last update
            </div>
        );
    }
}
